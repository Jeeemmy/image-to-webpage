# AGENTS.md

## 项目规则

- 每当在本项目中生成新页面时，必须严格秉持“每页面绝对独立”的原则。
- 即使新页面与之前生成的页面在主题、布局、组件、文案、样式或功能上存在相似之处，也不应该参照、复用、推断或借鉴之前页面的任何信息。
- 生成新页面时，只能依据当前任务明确提供的输入、素材、约束和项目通用工程规则进行实现。
- 不得把旧页面作为新页面的隐式参考、默认模板、设计基准、内容来源或质量对照。
- 如果当前任务没有提供足够信息，应基于当前任务本身做合理独立设计，而不是从已有页面中补全。

